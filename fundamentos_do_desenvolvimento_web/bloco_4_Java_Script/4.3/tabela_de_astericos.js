let largura = 5
let asterisco = '*'
let linha = ''

/* for (indexlinha = 0; indexlinha < largura; indexlinha += 1) {
    linha += asterisco;
};
for (indexlinha = 0; indexlinha < largura; indexlinha += 1) {
    console.log(linha);
}; */

let size = 5;
let symbol = '*';
let inputLine = '';
for (let lineIndex = 0; lineIndex <= size; lineIndex += 1) {
    console.log(inputLine);
    inputLine = inputLine +  symbol;
  };